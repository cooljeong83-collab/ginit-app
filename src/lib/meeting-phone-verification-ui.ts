/**
 * 모임 이용 시 전화번호(SMS) 인증 UI 표시 및 요구 여부.
 * `false`로 두면 화면에서 전화 인증 블록을 숨기고, 약관·필수 프로필 정보만으로 모임 인증 완료로 처리합니다.
 * 나중에 다시 켜려면 `true`로 변경하면 됩니다.
 */
export const MEETING_PHONE_VERIFICATION_UI_ENABLED = false;
